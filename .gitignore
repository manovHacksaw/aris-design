# ── Dependencies ─────────────────────────────
node_modules
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# ── Next.js (client) ─────────────────────────
client/.next/
client/out/
client/build/
client/next-env.d.ts
client/*.tsbuildinfo

# ── Root-level Next.js artifacts (should never be committed) ──
/.next/
/next-env.d.ts
/bun.lockb

# ── Server build ─────────────────────────────
server/dist/
server/build/

# ── Env files ────────────────────────────────
.env
.env.*
client/.env
client/.env.*
server/.env
server/.env.*

# ── Logs ─────────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
server/debug.log
server/reward_logs.txt

# ── Misc ─────────────────────────────────────
.DS_Store
*.pem
.vercel
coverage

oldFrontend/

client/service-account.json
mintingscripts
web3
lightmode

# ── DB migration (contains hardcoded credentials) ────
server/db_migration/

# ── Claude local settings ─────────────────────
.claude/settings.local.json
.claude/worktrees/